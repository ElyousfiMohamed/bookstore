<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha512-Fo3rlrZj/k7ujTnHg4CGR2D7kSs0v4LLanw2qksYuRlEzO+tcaEPQogQ0KaoGN26/zrn20ImR1DfuLWnOo7aBA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
            <style>
                .navbar-brand {
                    padding:20px;
                    margin:0;
                }
            </style>
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
        <!-- JavaScript Bundle with Popper -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="container">
            <!--<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3 mt-0 justify-content-center p-0">
                <a class="navbar-brand" href="{{ path('user_index') }}">User</a>
                <a class="navbar-brand" href="{{ path('auteur_index') }}">Auteur</a>  
                <a class="navbar-brand" href="{{ path('livre_index') }}" >Livre</a>
                <a class="navbar-brand" href="{{ path('genre_index') }}" style="border-right:1px white solid;margin-right : 200px;padding:20px">Genre</a>
                <a class="navbar-brand logout" href="{{ path('app_logout') }}" style="color:red;border-left:0px;">Logout <i class="fa fa-sign-out fa-xs" aria-hidden="true"></i>&nbsp;&nbsp;</a>|
            </nav>-->
            <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3" style="border-bottom:1px solid black;">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('livre_index') }}">
                    <i class="fas fa-book"></i>
                    BookStore
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('user_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Utilisateur
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('user_index') }}"><i class="fas fa-eye fa-xs"></i> Afficher</a></li>
                        {% if app.user and app.user.roles[0]=="ROLE_ADMIN"  %}
                            <li><a class="dropdown-item" href="{{ path('user_new') }}"><i class="fas fa-plus fa-xs"></i> Ajouter</a></li>
                        {% endif %}
                    </ul>
                    </li>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('auteur_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Auteur
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('auteur_index') }}"><i class="fas fa-eye fa-xs"></i> Afficher</a></li>
                        {% if app.user and app.user.roles[0]=="ROLE_ADMIN"  %}
                            <li><a class="dropdown-item" href="{{ path('auteur_new') }}"><i class="fas fa-plus fa-xs"></i> Ajouter</a></li>
                        {% endif %} 
                    </ul>
                    </li>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('livre_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Livre
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('livre_index') }}"><i class="fas fa-eye fa-xs"></i> Afficher</a></li>
                        {% if app.user and app.user.roles[0]=="ROLE_ADMIN"  %}
                            <li><a class="dropdown-item" href="{{ path('livre_new') }}"><i class="fas fa-plus fa-xs"></i> Ajouter</a></li>
                        {% endif %}     
                    </ul>
                    </li>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('genre_index') }}" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Genre
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('genre_index') }}"><i class="fas fa-eye fa-xs"></i> Afficher</a></li>
                        {% if app.user and app.user.roles[0]=="ROLE_ADMIN"  %}
                            <li><a class="dropdown-item" href="{{ path('genre_new') }}"><i class="fas fa-plus fa-xs"></i> Ajouter</a></li>
                        {% endif %}   
                    </ul>
                    </li>
                    <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Ressources
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="https://github.com/ElyousfiMohamed/bookstore/tree/master" target="_blank"><i class="fab fa-github fa-xs"></i> GitHub</a></li>
                        <li><a class="dropdown-item" href="https://www.youtube.com/watch?v=xCaq0K5WdoQ" target="_blank"><i class="fab fa-youtube fa-xs"></i> YouTube</a></li>
                    </ul>
                    </li>
                </ul>
                <form class="d-flex">
                    <a class="navbar-brand" href="{{ path('app_logout') }}">
                        Déconnecter
                        <i class="fas fa-door-open"></i>
                    </a>
                </form>
                </div>
            </div>
            </nav>
              {% block body %}
                {{ include('../livre/index.html.twig') }}
            {% endblock %}
        </div>
    </body>
</html>
